{
  "name": "@ai-usage/report",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@ai-usage/core": "workspace:*",
    "@ark-ui/solid": "^5.37.1",
    "@tanstack/solid-router": "^1.170.15",
    "@tanstack/solid-table": "^8.21.3",
    "solid-js": "^1.9.13"
  },
  "devDependencies": {
    "@pandacss/dev": "^1.11.3",
    "vite": "^8.0.16",
    "vite-plugin-solid": "^2.11.12"
  },
  "scripts": {
    "dev": "CI=1 panda codegen --silent && CI=1 panda cssgen --silent && vite --host 127.0.0.1 --open",
    "build": "CI=1 panda codegen --silent && CI=1 panda cssgen --silent && tsc --noEmit && vite build",
    "check": "CI=1 panda codegen --silent && CI=1 panda cssgen --silent && tsc --noEmit",
    "test": "bun test",
    "preview": "bun serve.ts --host 127.0.0.1",
    "lint": "biome check .",
    "format": "biome format --write ."
  }
}
