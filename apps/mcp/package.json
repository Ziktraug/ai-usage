{
  "name": "@ai-usage/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "ai-usage-memory-mcp": "./src/main.ts"
  },
  "dependencies": {
    "@ai-usage/mcp-adapter": "workspace:*",
    "@ai-usage/memory-service": "workspace:*",
    "@ai-usage/usage-engine-control": "workspace:*"
  },
  "scripts": {
    "start": "bun --no-env-file src/main.ts",
    "build": "tsc --noEmit",
    "check": "tsc --noEmit",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome format --write ."
  }
}
