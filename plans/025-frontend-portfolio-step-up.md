# Plan 025: Frontend portfolio step-up

**Planned at:** `main@6877bc4`, 2026-07-20. **Priority:** P0. **Effort:** L. **Risk:** MED. **Branch:** `agent/frontend-portfolio-step-up`. **Worktree:** `/tmp/ai-usage-frontend-portfolio`.

## Objective and constraints

Turn the public repo into immediate evidence of senior frontend work, then close audit gaps. Preserve Bun, Solid, TanStack, Panda, URL state, exact revisions, client-first/loopback-only live architecture and file-only Sync. Demo assets must use committed synthetic fixtures, never real histories, paths, prompts, machines, quotas, credentials or provider data. Do not add SSR migration, Storybook, new metrics, framework migration, HTML export or LAN sync.

Incorporates the 2026-07-19 frontend audit (session-run UI review + code audit). Steps 1–3 are the application-facing vitrine and form a valid pause point if the application deadline lands before the rest.

## Ordered implementation

1. **Baseline/decisions:** run `bun run check`, `lint`, `typecheck`, `test`, `build`, `test:web-production`, `test:e2e`, `test:e2e-production`; stop on pre-existing failure. Record owner choices for license (MIT recommended) and completed-plan curation (concise HEAD history with details recoverable from git recommended).
2. **Privacy-safe demo:** add typed `live | e2e | demo` runtime owner and `bun run demo` with distinct `VITE_AI_USAGE_DEMO=1`. Reuse synthetic `demoReportPayload`; inject an inert client with no SSE/fetch/collectors/config/history/skills/publication/mutation. Guard Skills/Sync/Sources; label Demo data. Test boundaries.
3. **Showcase/story:** deterministically capture Overview desktop 1440×1100 in both themes, the open session drawer, and mobile 390×844 from demo fixtures with reduced motion; commit optimized `docs/assets` images <1 MiB. Reorder README: value, visuals, demo privacy quick start, frontend highlights (name the concrete evidence: hand-rolled focus-trap dialog, roving-tabindex heatmap, DI-testable SSE client, CI-enforced package boundaries), transferable architecture, then existing detail. Set repo metadata with the owner via `gh repo edit`: description, topics (`solidjs`, `tanstack`, `panda-css`, `bun`, `data-visualization`, `developer-tools`), homepage when a demo URL exists. Add a linked human-owned case study covering constraints, UX/a11y/state/performance decisions, rejected alternatives, tests, exact AI assistance and maintainer review. Apply owner-approved plan-history policy without concealing AI or rewriting history.
4. **A11y/responsive:** make text token pairs reach 4.5:1 in both themes with tests; give Punchcard a named structured non-empty-cell alternative without 168 tab stops/`title` dependence; preserve heatmap roving focus while meeting WCAG 2.5.8 24px target/spacing; add shared AppHeader with stable Primary nav/current page/separate actions/wrapping; add reduced-motion. E2E every route at 361/390px for one nav/H1 and no overflow.
5. **Regression/public hygiene:** fix the load-time 404 console resource error, then fail e2e on new console errors; add axe scans for Overview, Sessions, Breakdown, drawer, Skills, Sources, Sync with no blanket suppression; keep 3–5 deterministic visual baselines. Add component render tests (`@solidjs/testing-library` under `bun test`) for the discard-confirmation dialog (trap cycle, Escape, focus restore) and the Overview heatmap (roving tabindex, arrow-key movement) — these lock the a11y showcase claims from step 3. Wire design-system `bun test src`; remove duplicate CI tool tests; add CONTRIBUTING/SECURITY and only the owner-approved LICENSE.
6. **Scroll-first sessions/perf (redirected by owner, 2026-07-20):** scroll stays the only gesture; do not build request/generation lock ownership. Measure whether the full filtered row set for a realistic store (~5k sessions) loads in one query with the hand-rolled window carrying render cost (bounded DOM count, memory, first-interaction timing, both surfaces — mobile cards are currently unwindowed). If measurements pass, remove incremental paging outright; otherwise keep the existing transparent prefetch (desktop 12-row lookahead, mobile sentinel) unchanged and add one regression e2e proving a generation change mid-scroll never duplicates or drops rows. Keep `@tanstack/solid-table` and the hand-rolled fixed-height window; do not reintroduce `@tanstack/solid-virtual` while rows stay fixed-height (added `bfd00f6`, removed `9eb4853` after microtask re-measure hacks) — record this as an ADR and case-study material. Budget the gzip Vite static-import closure (<=10% above measured baseline, target <=235KB); split exports only when measured.
7. **Characterized extraction (separate/deferrable):** first test URL reload, exact-revision supersession/no-op, responsive drawer focus and range pointer/keyboard behavior. Extract only narrow typed owners from Dashboard/TimeRangeControl while preserving Solid effect order/reactivity. Stop if no behavior-preserving seam exists. Once both files pass it, re-enable `noExcessiveCognitiveComplexity` in `biome.json` — the disabled rule is itself an audit finding and must not outlive the extraction.
8. **Idiom consistency (separate/deferrable):** migrate `routes/index.tsx` from imperative `onMount` loading and the mutable publication-retry flag to a TanStack Router loader with equivalent retry semantics; move `skills-detail.tsx`'s 22 inline `css()` blocks onto design-system slots to match `overview.tsx`/`time-range-control.tsx`. Behavior-preserving only; verify with existing e2e.

## Done/STOP

Done when all gates pass; demo is synthetic/labeled/read-only; README/case study expose frontend and honest AI/human ownership; repo description/topics are set with the owner; license/history follow owner decisions; a11y/visual/axe/render-test/scroll-behavior/budget gates pass; design-system tests run once; no real data enters assets. Steps 7–8 may remain deferred without blocking Done, but `noExcessiveCognitiveComplexity` stays off only while step 7 is deferred. STOP if demo needs live/local/provider/hosting access, owner decisions are absent, a11y changes calculations, paging needs server/revision contracts, budget needs speculative churn, extraction changes semantics without characterization, or any gate fails twice.
