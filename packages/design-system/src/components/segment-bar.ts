import { css } from '@ai-usage/design-system/css';

export const barTrack = css({
  h: '6px',
  mt: '8px',
  borderRadius: 'full',
  bg: 'track',
  overflow: 'hidden',
});

export const barFill = css({ h: '100%', borderRadius: 'full' });
